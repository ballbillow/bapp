<?xml version="1.0" encoding="utf-8"?>

<glog>
	<!--
		GLOG_DEFINE_bool(logtostderr, BoolFromEnv("GOOGLE_LOGTOSTDERR", false),
                 "log messages go to stderr instead of logfiles");
	-->
	<logtostderr>0</logtostderr>
	<!--
		GLOG_DEFINE_bool(alsologtostderr, BoolFromEnv("GOOGLE_ALSOLOGTOSTDERR", false),
                 "log messages go to stderr in addition to logfiles");
	-->
	<alsologtostderr>0</alsologtostderr>
	<!--
		GLOG_DEFINE_string(alsologtoemail, "",
                   "log messages go to these email addresses "
                   "in addition to logfiles");
	-->
	<alsologtoemail>billowqiu@163.com</alsologtoemail>
	<!--
		GLOG_DEFINE_bool(log_prefix, true,
                 "Prepend the log prefix to the start of each log line");

	-->
	<log_prefix>1</log_prefix>
	<!--
		GLOG_DEFINE_int32(minloglevel, 0, "Messages logged at a lower level than this don't "
                  "actually get logged anywhere");
	-->
	<minloglevel>0</minloglevel>
	<!--
		GLOG_DEFINE_int32(logbuflevel, 0,
                  "Buffer log messages logged at this level or lower"
                  " (-1 means don't buffer; 0 means buffer INFO only;"
                  " ...)");
	-->
	<logbuflevel>0</logbuflevel>
	<!--
		GLOG_DEFINE_int32(logbufsecs, 30,
                  "Buffer log messages for at most this many seconds");
	-->
	<logbufsecs>3</logbufsecs>
	<!--
		GLOG_DEFINE_int32(logemaillevel, 999,
                  "Email log messages logged at this level or higher"
                  " (0 means email all; 3 means email FATAL only;"
                  " ...)");
	-->
	<logemaillevel>999</logemaillevel>
	<!--
		GLOG_DEFINE_string(logmailer, "/bin/mail",
                   "Mailer used to send logging email");
	-->
	<logmailer>/bin/mail</logmailer>
	<!--
		GLOG_DEFINE_string(log_dir, "D://glog_dir"/*DefaultLogDir()*/,
                   "If specified, logfiles are written into this directory instead "
                   "of the default logging directory.");
	-->
	<log_dir>./log</log_dir>
	<!--
		GLOG_DEFINE_string(log_link, "", "Put additional links to the log "
                   "files in this directory");
	-->
	<log_link></log_link>
	<!--
		GLOG_DEFINE_int32(max_log_size, 1800,
                  "approx. maximum log file size (in MB). A value of 0 will "
                  "be silently overridden to 1.");
	-->
	<max_log_size>1800</max_log_size>
	<!--
		GLOG_DEFINE_bool(stop_logging_if_full_disk, false,
                 "Stop attempting to log to disk if the disk is full.");
	-->
	<stop_logging_if_full_disk>0</stop_logging_if_full_disk>
	<!--
		GLOG_DEFINE_string(log_backtrace_at, "",
                   "Emit a backtrace when logging at file:linenum.");
	-->
	<log_backtrace_at></log_backtrace_at>
</glog>
